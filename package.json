{
    "scripts": {
        "clean": "rimraf dist",
        "copy": "cpx \"src/**/*\" dist",
        "build:css": "tailwindcss -i src/assets/css/input.css -o dist/assets/css/site.css",
        "build": "npm run clean && npm run build:css && npm run copy"
    },
    "devDependencies": {
        "@tailwindcss/cli": "^4.1.18",
        "rimraf": "^6.0.0",
        "cpx": "^1.5.0"
    }
}
